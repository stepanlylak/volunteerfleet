# ─── Production .env для docker-compose.prod.yml ──────────────────────────────
# Скопіюй у `.env` поряд із docker-compose.prod.yml на сервері та заповни значення.
# Усі секрети — згенеруй випадково (напр. `openssl rand -hex 32`).

# ─── Образ і мережа ───────────────────────────────────────────────────────────
# Тег релізу з container registry (див. docs/deployment.md).
VF_IMAGE=ghcr.io/your-org/volunteerfleet:1.0.0
# Зовнішня Docker-мережа, де працює Zoraxy (має існувати до `up`).
SHARED_NETWORK=n8n_default

# ─── App ──────────────────────────────────────────────────────────────────────
NODE_ENV=production
PORT=3000
# Домен фронтенду/API (один origin). Має збігатися з тим, що проксіює Zoraxy.
CORS_ORIGIN=https://fleet.example.com

# ─── Database ───────────────────────────────────────────────────────────────--
POSTGRES_USER=volunteerfleet_user
POSTGRES_PASSWORD=CHANGE_ME_strong_password
POSTGRES_DB=volunteerfleet
# Хост = ім'я контейнера postgres у внутрішній мережі (НЕ localhost, НЕ 55432).
DATABASE_URL=postgresql://volunteerfleet_user:CHANGE_ME_strong_password@volunteerfleet-postgres:5432/volunteerfleet

# ─── Auth (секрети ≥ 32 символи) ───────────────────────────────────────────────
JWT_ACCESS_SECRET=CHANGE_ME_to_32_plus_random_chars_xxxxxxxxxx
JWT_ACCESS_TTL=15m
JWT_REFRESH_SECRET=CHANGE_ME_too_to_another_32_plus_random_xxxx
JWT_REFRESH_TTL=30d
BCRYPT_COST=12

# ─── First admin (створюється один раз при першому старті) ──────────────────────
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=CHANGE_ME_on_first_login
ADMIN_NAME=Адміністратор

# ─── S3 / MinIO ─────────────────────────────────────────────────────────────--
# S3_ENDPOINT — ПУБЛІЧНИЙ URL, який Zoraxy проксіює на контейнер volunteerfleet-minio.
# Браузер ходить за presigned-посиланнями саме сюди, тож це має бути доступний ззовні
# домен (а Zoraxy має ПЕРЕДАВАТИ оригінальний Host, інакше підпис presigned не зійдеться).
S3_ENDPOINT=https://files.example.com
S3_REGION=us-east-1
S3_BUCKET=volunteerfleet-documents
# Креди застосунку до MinIO = root-креди MinIO (значення мають збігатися).
S3_ACCESS_KEY=volunteerfleet_minio
S3_SECRET_KEY=CHANGE_ME_minio_password
MINIO_ROOT_USER=volunteerfleet_minio
MINIO_ROOT_PASSWORD=CHANGE_ME_minio_password

# ─── Misc ───────────────────────────────────────────────────────────────────--
EXCHANGE_RATES_FILE=./data/exchange-rates.json
MAX_UPLOAD_BYTES=26214400

# ─── Примітка ───────────────────────────────────────────────────────────────--
# VITE_*-змінні тут НЕ потрібні: фронтенд збирається в образ із відносним /api/v1
# і віддається тим самим контейнером (один origin).
